using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class UIManagerString1 : MonoBehaviour
{
    /*
    // Necesitamos acceder al Input Field
    public TMP_InputField usernameInputField;

    // Función que nos permitirá guardar el nombre de usuario para poder disponer de él en otras escenas
    public void SaveUserName()
    {
        // Primero obtenemos el username introducido en el Input Field
        string _username = usernameInputField.text;
        
        // Luego, guardamos el nombre de usuario para poder usarlo en otras escenas
        PersistentDataStringTutorial.sharedInstance.username = _username;
    }
    */
}
